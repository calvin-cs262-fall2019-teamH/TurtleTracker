import React from 'react';
import { View, Text } from 'react-native';

export default class TurtleAddProfileScreen extends React.Component {
    render() {
        return (
            <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
                <Text>Turtle Add Profile Screen</Text>
            </View>
        );
    }
}